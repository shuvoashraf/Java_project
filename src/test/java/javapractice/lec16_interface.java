package javapractice;

interface lec16_interface {
	
	// interface also hide code
	//interface completely a abstract class
	//interface allow only interface method
	//interface consider as a class,abstract class
	//so we use interface before class name instead of class
	//but in method don't need to write interface keyword inside method

	// interface is a class ,so we write interface instead of class
	
	
	public void method1();  //interface method does not have body)
	public void method2();
	

	


	}

